## max-age=60, must-revalidate

1. 第一次访问：返回`200`
2. 第二次访问，假设距离距离第一次访问的时间
    a、小于等于60秒：`200`，from cache
    b、大于60秒：向缓存服务器发起请求；（缓存服务器逻辑如下）

缓存服务器逻辑：

1. 未过期：命中缓存，直接返回
2. 已过期：到源站重新校验
  a、未过期：返回304
  b、已过期：重新拉取，更新本地副本，返回200

## no-cache

1. 缓存服务器：到源站校验
  a、未过期：返回304
  b、已过期：重新拉取，更新本地副本，返回200


